# web/

独立站的前端，待建。产品形态看 `../docs/demo.html`（可点原型，本地假数据）。

约束：
- 作品集模板的内容是 `profiles.content` 里的 JSON，由前端按模板渲染，不接受用户 HTML。
- 调 API 走 `Authorization: Bearer <墙 JWT>`；独立成站后再加自己的登录。
- 视觉沿用各校墙的 token（近黑海军蓝、4px 控件、14px 卡片），荧光黄只用来标"开放组队"。
